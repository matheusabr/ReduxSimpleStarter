// We need to import ReactDOM
// cause it will transpile
// the JSX to React.createElement
import React from 'react';

// It is called React Functional component
// cause it is a function
const SearchBar = () => {
  return <input />;
};

export default SearchBar;
